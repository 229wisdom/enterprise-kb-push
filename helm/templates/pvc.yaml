# 数据卷：文档原件 + 向量库 + SQLite 持久化
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ekb-data
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ .Values.persistence.size }}

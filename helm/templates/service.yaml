# 服务暴露：集群内访问入口
apiVersion: v1
kind: Service
metadata:
  name: enterprise-kb
spec:
  type: ClusterIP
  selector:
    app: enterprise-kb
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
